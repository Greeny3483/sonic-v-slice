import flixel.FlxG;
import flixel.tweens.FlxTween;
import flixel.util.FlxTimer;
import funkin.play.PlayState;
import funkin.play.stage.Stage;
import funkin.Conductor;

import EXEShaders;
import EXEStrumlines;

class SpotlightStage extends Stage
{
    public function new()
    {
        super('spotlight');
    }

    override function onCreate(event:ScriptEvent):Void {
        super.onCreate(event);

        EXEShaders.initShader("distortion");
    }

    override function onStepHit(event:SongTimeScriptEvent):Void {
        super.onStepHit(event);

        if (PlayState.instance.currentSong.id.toLowerCase() != "sunshine") return;

        switch (event.step) {
            case 588:
                getNamedProp('Background').alpha = 0;
                getBoyfriend().alpha = 0;
                getDad().alpha = 0;

                EXEStrumlines.changeStrumlineMode("middlescroll");
            case 592:
                getDad().alpha = 1;
            case 860:
                getNamedProp('Background').alpha = 1;
                getBoyfriend().alpha = 1;
            
                EXEStrumlines.resetStrumlines("playerX");
                EXEStrumlines.resetStrumlines("opponentX");
                PlayState.instance.playerStrumline.alpha = 1; // for mobile
        }
    }

    override function onUpdate(event:UpdateScriptEvent):Void {
        super.onUpdate(event);

        EXEShaders.updateShader("distortion");
    }
}