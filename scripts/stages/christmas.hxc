import flixel.FlxG;
import funkin.play.stage.Stage;

class ChristmasStage extends Stage
{
	public function new()
	{
		super('christmas');
	}

	override function buildStage()
	{
		super.buildStage();

		if (FlxG.onMobile) {
			getNamedProp('Foreground').scale.set(1.1, 1.0);
		}
	}
}
